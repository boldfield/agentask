apiVersion: v1
kind: Secret
metadata:
  name: agentask-secret
type: Opaque
stringData:
  # IMPORTANT: This is a placeholder value. Replace with an actual token before deploying to production.
  # Generate a secure token and update this value.
  token: "placeholder-token-replace-me-in-production"
